name: backend pipeline

on:
  pull_request: {}
  push:
    branches:
      - main
    paths-ignore: 
      - "README.md" 
      - "docs/**" 
      - "frontend/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2 
        with: 
          node-version: '12'
      - run: npm install
      - run: npm test